# Needed to import libs core and gui
QT += core gui
# Specifies that the version of QT > 4
greaterThan(QT_MAJOR_VERSION, 4) : QT += widgets
# Specifies what C++ file is main
SOURCES += \
    main.cpp
